# name (not path) of data file
# assume that this file is in directory "data"
data_file_name = "example_data.txt"

# precision of float type when comparing ">", "<", and "=="
eps = 0.000001

# RBF Kernel:
# RBF(x) = exp(-norm(x) ** 2 / (2 * sigma ** 2))
# default value of sigma
sigma_deflt = 2.0

# default value of penalty term
C_deflt = 0.6

# plot margin in three dimensions
plt_d1_margin = 1
plt_d2_margin = 1
plt_d3_margin = 1
